<FlexibleOneLineAvatarListItem>:
    MDBoxLayout:
        padding: dp(20), dp(0), dp(0), dp(0)
        spacing: dp(15)
        radius: root.radius
        MDIcon:
            size_hint_x: 0.15
            icon: root.icon
        MDLabel:
            text: root.text
            halign: "left"

