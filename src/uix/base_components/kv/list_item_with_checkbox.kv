
<ListItemWithCheckbox>:
    text: self.name * 1000
    secondary_text: self.description * 1000
    on_release: root.details(root.name, root.description, root.image)

    LeftImage:
        icon: root.image

    IconRightWidget:
        icon: root.icon
